name: test_glob_star_match
limit: 5000
memory_size: 0x2000

memory_mapped_io:
  0x80: ["a", "*", "c", "\n", "a", "b", "c", "\n"]
  0x84: []

reports:
  - name: Result
    slice: last
    view: |
      numio[0x80]: {io:0x80:dec}
      numio[0x84]: {io:0x84:dec}
    assert: |
      numio[0x80]: [] >>> []
      numio[0x84]: [] >>> [1]